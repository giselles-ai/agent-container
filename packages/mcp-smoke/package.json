{
  "name": "@giselles/mcp-smoke",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "giselles-mcp-smoke": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "check": "pnpm run build && node dist/index.js",
    "check:discovery": "pnpm run build && node dist/index.js --mode discovery",
    "check:sandbox": "pnpm run build && node dist/index.js --target sandbox",
    "check:sandbox:discovery": "pnpm run build && node dist/index.js --target sandbox --mode discovery"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@vercel/sandbox": "^1.5.0"
  },
  "devDependencies": {
    "@types/node": "25.0.10",
    "typescript": "5.9.3"
  }
}
